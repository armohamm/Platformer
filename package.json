{
	"name": "platformer",
	"version": "1.0.0",
	"description": "A TypeScript library for managing infrastructure remotely on various cloud providers",
	"main": "bundle.js",
	"license": "MIT",
	"scripts": {
		"compile": "tsc && rollup -c",
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"devDependencies": {
		"@types/jest": "^24.0.9",
		"@types/node": "^11.10.4",
		"rollup-plugin-commonjs": "^9.2.1",
		"rollup-plugin-node-resolve": "^4.0.1",
		"rollup-plugin-sourcemaps": "^0.4.2"
	}
}
